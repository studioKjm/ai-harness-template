fastapi:
  - from: "src/api/routes"
    cannot_import: ["create_engine", "sessionmaker"]
    reason: "라우트에서 DB 세션 직접 생성 금지"
    stacks: [fastapi]
    file_patterns: ["*.py"]

general:
  - from: "."
    cannot_import: ["os.system", "subprocess.call"]
    reason: "명령어 인젝션 위험. subprocess.run(shell=False) 사용"
    stacks: [python]
    file_patterns: ["*.py"]
